{
  "name": "@lumen/pipelines",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run modal serve app.py",
    "test": "uv run pytest tests/",
    "lint": "uv run ruff check .",
    "lint:fix": "uv run ruff check --fix .",
    "setup": "uv sync"
  }
}
